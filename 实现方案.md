# ZCode 火山方舟模型 Token 用量实时统计 — 调研与实现方案

> 2026-08-31 · 调研阶段产出，待确认后开工

---

## 一、本机数据源勘察结论（方案的地基）

在动手选型前，先确认了「数据从哪来」。结论：**ZCode 已经把每次模型调用的用量完整记录在本地 SQLite 里，仪表盘零侵入、只读挂库即可**。

### 1.1 核心数据源：`~/.zcode/cli/db/db.sqlite`（WAL 模式）

| 表 | 行数 | 内容 |
|---|---|---|
| `model_usage` | 947 | **每次模型请求一行**：`provider_id`、`model_id`、`variant`、`agent`、`query_source`、`status`、`started_at`/`completed_at`（毫秒 epoch）、`duration_ms`、`time_to_first_token_ms`、`input_tokens`、`output_tokens`、`reasoning_tokens`、`cache_creation_input_tokens`、`cache_read_input_tokens`、`computed_total_tokens`、`finish_reason`、`retry_count`、`error_type`/`error_message`、`raw_usage_json` |
| `turn_usage` | 76 | 每轮对话聚合（请求数、token 汇总、耗时） |
| `session` | 47 | 会话标题、目录、起止时间 → 可算"最长聊天时长/连续活跃天数" |
| `tool_usage` | 998 | 工具调用明细（可选扩展） |
| `schema_migration` | 18 | 版本迁移记录 → 解析器可按版本适配 |

已验证的供应商覆盖（实测本机数据）：
- `builtin:zai-start-plan`：GLM-5.3、GLM-5.3-Flash（智谱计划）
- `c528fecb-…`（自定义供应商 UUID，即**火山方舟 Ark** 网关）：`glm-5.3[1m]`、`glm-5.3-flash[1m]`

即：**方舟自定义供应商的请求同样被完整记录**，无需任何代理或拦截。

### 1.2 辅助数据源

- `~/.zcode/v2/config.json`：供应商配置（名称、enabled、baseURL、模型列表含 `limit`（上下文长度，如 1M）、`modalities`（视觉标签））→ 图1"模型设置"页可直接读它渲染（**只读，绝不展示/存储其中的 apiKey**）
- `~/.zcode/cli/rollout/model-io-sess_*.jsonl`：每次模型 IO 的原始请求/响应（含 usage），可作对账/调试用
- `~/.zcode/cli/agents/sess_*/agent_*/transcript.jsonl`：子代理转录，同样含 usage + model
- `~/.zcode/cli/log/zcode-YYYY-MM-DD.jsonl`：CLI 运行日志
- `~/.zcode/v2/coding-plan-cache.json`：仅有各内置计划"可用/不可用"状态，**没有余额数字**

### 1.3 技术要点（坑与对策）

- `model_usage.id` 是字符串主键（无自增）→ 增量拉取用 SQLite `rowid`
- WAL 模式：以 `mode=ro` 只读连接 + `busy_timeout` 并发读是安全的（已实测）
- 时间戳为毫秒 epoch；错误请求行 token 全为 0，统计时要按 `status` 过滤
- 模型名归一化：`glm-5.3-flash[1m]` → 显示名 `GLM-5.3-Flash`（建一张映射表）

---

## 二、GitHub 同类开源项目对比

> star 数为本次抓取 README 时的近似值。

| 项目 | 定位 / UI | 技术栈 | 用量数据来源 | 实时性 | Stars | 对本项目的不足 |
|---|---|---|---|---|---|---|
| **ccusage** (ryoppippi) | CLI 报表（daily/weekly/monthly/session/blocks）+ statusline | TypeScript (pnpm monorepo) + Rust 辅助，Bun/Node | 解析本地会话 JSONL，支持 16 种 CLI（Claude Code、Codex、Gemini CLI…**不含 ZCode**） | blocks 有活动监控，整体偏离线 | ≈18.2k | 无 Web 图表；不支持 ZCode；改造需深入其 monorepo 且界面非图2样式 |
| **claude-monitor** (Maciek-roboblog) | 终端 TUI 实时监控（进度条、burn rate、限额预测） | Python + rich + pydantic | watch Claude Code 本地 JSONL（`~/.config/claude`）+ statusline 数据 | 实时（1–60s 刷新） | ≈8.7k | 纯 TUI 无 Web；概念绑定 Claude 5h 计费窗口/订阅限额，不适配方舟按量 |
| **claude-code-router** (musistudio) | 本地网关(:3456) + `ccr ui` Web 管理页 | Node 22+ / TS / Electron / better-sqlite3 | 代理侧计数（请求经它转发） | 实时 | ≈37k | 需把 ZCode 上游改指向它，链路加长；统计图表维度未确认；Windows 打包受限 |
| **new-api** (QuantumNous) | LLM 网关 + 完整中文控制台（渠道/令牌/额度/统计分析） | Go + Web 前端，默认 SQLite，可 Docker | 代理侧计费中间件 | 实时 | ≈46.8k | 运营向概念（多用户/充值/渠道）对单人过重；AGPLv3 附加条款；同样要改上游指向 |
| **LiteLLM** (BerriAI) | 网关 + spend tracking + Admin UI | Python 网关 + Rust 核心，需 Postgres + Redis + Docker | 代理侧写 DB | 实时 | ≈57.6k | 部署最重（本机无 Docker），团队平台定位 |
| **Helicone / Langfuse** | 平台级 LLM 可观测 | Docker + ClickHouse/Postgres | 代理/SDK 上报 | 实时 | 5w+/9w+ 量级 | 单人本地场景明显杀鸡用牛刀 |
| **cc-switch** (farion1231) | 桌面"供应商管理器"：供应商切换、MCP/提示词/技能、会话管理、**用量仪表盘（趋势图/请求日志/自定义单价）** | **Tauri 2 + React 18 + TS + Vite + Tailwind + shadcn/ui + SQLite** | 自身代理/配置文件 | 实时 | ≈130k | 桌面应用；支持的 8 种 CLI 不含 ZCode；数据模型是它自己的 |

### 结论

1. **没有现成项目能"直接用"**：要么数据源不支持 ZCode（ccusage/claude-monitor/cc-switch），要么要把请求改道网关且部署重（router/new-api/LiteLLM/Helicone）。
2. **自研成本反而最低**：因为 ZCode 已把用量落成结构化 SQLite 表，我们只需"只读 + 聚合 + 展示"，核心工作量比改造上面任何一家都小，且能 1:1 复刻你给的两张界面。
3. **值得抄的作业**：
   - cc-switch 的技术栈与 UI（Tauri/React/Tailwind/shadcn、卡片式布局、SQLite 单一数据源）——图1 那种质感
   - ccusage 的统计口径与报表维度（daily/weekly/monthly、按模型 breakdown）
   - claude-monitor 的"实时刷新 + 数据来源可信度标注"思路
   - new-api 的按模型/按渠道聚合维度

---

## 三、推荐实现方案

### 3.1 架构

```
ZCode CLI / ZCode 桌面端
   │  (正常使用，零改动)
   ▼
~/.zcode/cli/db/db.sqlite (WAL)          ~/.zcode/v2/config.json
   │                                        │
   │ 只读连接 + rowid 增量轮询(2s)           │ 只读解析(供应商/模型元数据)
   ▼                                        ▼
┌─────────────────────────────────────────────┐
│  zcode-usage 本地服务 (Node.js 20+ / TS)     │
│  ├ collector：增量采集 → 聚合缓存            │
│  ├ API：Fastify REST + SSE(/api/events)     │
│  └ 静态托管前端构建产物                       │
└─────────────────────────────────────────────┘
   ▼ http://localhost:5323
前端 (React 18 + Vite + TS + Tailwind + shadcn/ui + ECharts)
```

- **采集**：不解析日志、不做代理。对 `model_usage` 按 `rowid > 上次水位` 增量查询，内存/轻量缓存做日/周/月聚合；监听 `db.sqlite-wal` mtime 变化触发，兜底定时器 2s。
- **实时推送**：SSE 把增量事件推给前端，前端局部更新，无需手刷。
- **不写 ZCode 的任何文件**：全程只读（config.json 只读渲染，杜绝破坏 ZCode 配置）。

### 3.2 页面设计（对齐两张参考图）

**页面 1：使用统计（复刻图2）**
- 顶部 5 张统计卡：累计 Token 数 / 峰值 Token 数（单日最高）/ 最长聊天时长 / 当前连续活跃天数 / 最长连续活跃天数
- Token 活动：ECharts calendar 热力图，右上角 每日/每周/累计 切换
- 时间范围（近 7 日 / 近 30 日）+ 每日 Token 趋势图：按模型多色折线
- 模型用量：环形图（中心显示总量）+ 右侧明细列表（模型名、tokens、占比）

**页面 2：模型设置（复刻图1）**
- 左侧：供应商列表（Z.ai、火山方舟 Ark、+ 添加供应商〔二期〕）
- 右侧：供应商名 + 已启用徽章（读 config.json `enabled`）、连接方式、
  今日余额卡片：每模型进度条（今日已用 / 额度、百分比、重置时间 23:59）
  - 今日已用 = `model_usage` 当日汇总（按 provider+model 过滤）
  - 额度 = 手动配置（默认按图1：GLM-5.3 300万/日、Flash 500万/日）；二期可尝试对齐 ZCode 桌面端同源的 zcode.z.ai 额度接口自动获取
- 模型列表：模型名 + 上下文长度标签（1M）+ 视觉标签（读 config.json `limit`/`modalities`）

### 3.3 技术选型与理由

| 层 | 选择 | 理由 |
|---|---|---|
| 运行时 | Node.js（本机已装 v25）+ TypeScript | 本机无 Docker/pnpm/bun；前后端同语言一套仓 |
| 后端 | Fastify + better-sqlite3(只读) + SSE | 轻量、单进程、Windows 友好 |
| 前端 | React 18 + Vite + Tailwind + shadcn/ui | 与 cc-switch 同款生态，卡片式 UI 复刻成本低 |
| 图表 | ECharts | calendar 热力图/环形图/折线图开箱即用，中文文档全 |
| 存储 | 不新增库，聚合走内存缓存 + 可选物化表 | 数据量小（单用户，几千行/月），SQLite 直查足够 |

### 3.4 备选形态（供对比）

| 方案 | 优点 | 缺点 |
|---|---|---|
| **A. 本地 Web 服务（推荐）** | 零依赖装 Node 即跑、浏览器访问、开发快 | 无原生窗口感 |
| B. Tauri 桌面应用 | 最像图1的原生质感、可托盘常驻 | 需装 Rust 工具链，打包调试成本高 |
| C. Python FastAPI + Vue | 本机有 Python 3.10 | 前端生态/组件复用不如 React 方案 |
| D. 网关代理计数（new-api/自写） | 能拿到服务端视角、支持多客户端 | 要改 ZCode 上游指向、重 |

A 先行，B（套壳）与 D（二期增强：成本核算/多客户端）不冲突，可后补。

### 3.5 里程碑

| 阶段 | 内容 | 预估 |
|---|---|---|
| M1 数据接入 | 只读 collector + 聚合查询 + REST/SSE | 0.5–1 天 |
| M2 使用统计页 | 5 统计卡、热力图、趋势图、环形图 | 1 天 |
| M3 模型设置页 | 供应商/模型渲染、额度配置、今日余额进度条 | 0.5–1 天 |
| M4 打磨 | 成本换算(¥，价格表可编辑)、CSV 导出、开机自启/托盘、一键启动脚本 | 1 天 |

### 3.6 风险与对策

| 风险 | 对策 |
|---|---|
| ZCode 的 SQLite schema 随版本演进 | 采集器按 `schema_migration` 版本探测，字段缺失时降级聚合 |
| DB 被 ZCode 独占/锁定 | WAL 只读连接 + busy_timeout + 失败重试（已实测可并发读） |
| 火山方舟无公开的 Token 用量/配额查询 API（文档站 JS 渲染未能确认，待实施时验证） | 完全用本地计数兜底；方舟侧仅展示"本地已用"，不承诺服务端数字 |
| Z.ai 计划"今日余额"服务端接口未验证（coding-plan-cache.json 无余额） | 一期手动配置额度+本地计数；二期抓包对齐桌面端同源接口 |
| 模型名归一化（`glm-5.3[1m]` vs `GLM-5.3`） | 显示名映射表，可在设置页维护 |
| 统计口径歧义 | 提供口径说明：总量=input+output(+reasoning)，cache 另列可开关 |

---

## 四、待确认的问题

1. **形态**：本地 Web（推荐，`npm start` 后浏览器打开）还是 Tauri 桌面应用（原生窗口/托盘）？
2. **技术栈**：Node/TS（推荐，环境已就绪）还是 Python？
3. **"今日余额"额度**：一期先手动配置 + 本地计数（稳），还是希望我先花时间验证 zcode.z.ai 的额度接口（效果同图1，但依赖逆向、且用你的登录态）？
4. **是否需要成本(¥)换算与 CSV 导出**（M4）？
5. **统计口径**：图2 的"1.1亿"看起来不含 cache read，是否按 input+output 对齐？
