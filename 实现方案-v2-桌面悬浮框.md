# v2.0 桌面悬浮框（火山方舟网关总额度）— 调研与实现方案

> 2026-09-01 · 待确认后动手。网页版（127.0.0.1:5323）为 v1.0，本方案交付桌面悬浮框 v2.0。

---

## 一、GitHub 同类项目对比

### A. 直接同类：LLM/Token 用量悬浮窗、托盘

| 项目 | Stars | 形态 | 技术栈 | 数据来源 | 关键实现 | 借鉴点 |
|---|---|---|---|---|---|---|
| **Javis603/token-monitor** | **1,867★** | Win 托盘 popover + 可拖动悬浮球 + 完整设置 GUI | **Electron** + tokscale（本地日志/SQLite 解析） | 读本地日志/DB（33+ 工具，**含 ZCode/GLM**）+ API Key 查配额（OpenRouter/Minimax/**火山引擎**） | 透明毛玻璃、悬浮球 hover/click 预览、全局热键、开机自启、SSE 同步、electron-builder 打包 | **形态与我们的目标几乎一致**；"悬浮球 + 悬停展开"交互范式；证明 Electron 是此类应用主流 |
| **SlavomirDurej/claude-usage-widget** | 311★ | Windows 置顶可拖动 widget | **Electron**（原生 HTML/CSS/JS） | Claude.ai API（浏览器登录捕获 session） | 置顶可开关、托盘专属模式、阈值三色（紫/橙75%/红90%）、Compact 紧凑模式、开机自启、托盘右键菜单、设置面板 | 交互全套模板：拖动/托盘菜单/阈值变色/自启的现成做法 |
| **aqua5230/usage** | 306★ | macOS 菜单栏 + **Win 托盘 + WebView2 主题面板** | Python | 被动读本地日志（不调 LLM API） | 托盘图标随配额百分比更新、HTML 面板 14 套主题、阈值系统通知、绿色版 exe | "托盘常驻 + HTML 面板弹窗"混合形态；Python 路线可行性佐证 |
| babakarto/CodexBar-Win | 100★ | Win 托盘 + customtkinter 弹窗 | Python（PyInstaller ≈30MB） | 四级 fallback（PTY→OAuth→浏览器 cookies→JSONL） | 托盘 + 原生弹窗；无自启 | 轻量 Python 路线参考 |
| jlaiii/deepseek-balance-widget | 0★ | 置顶悬浮卡显示 API 余额 | Python | DeepSeek API | 形态与"余额悬浮框"完全同构 | 小而全的 UI 参考 |
| zhongyang219/TrafficMonitor | **45,963★** | Windows 悬浮窗标杆（网速/CPU/内存 + 任务栏 + 皮肤） | C++/Win32 | 性能计数器 | 置顶、鼠标穿透、拖动、皮肤系统 | Windows 悬浮窗交互的"标准答案"（拖动/右键/穿透范式） |

### B. 悬浮窗技术框架类

| 项目 | Stars | 技术栈 | 关键实现 | 说明 |
|---|---|---|---|---|
| overlayeddev/overlayed | 673★ | **Electron**/TS | 置顶 + 鼠标穿透 + 多显示器 | Electron 做覆盖层（overlay）的成熟案例 |
| SeakMengs/WindowPet | 660★ | **Tauri 2** + React | 透明置顶无边框、点击穿透、开机自启 | Tauri 路线可行性佐证 |
| redheadesign/music-island | 146★ | Tauri（Rust） | Windows 顶部悬浮岛 | Tauri 原生质感案例 |

### 结论

1. **同类项目的主流技术栈是 Electron**（token-monitor、claude-usage-widget、overlayed），其次 Python 托盘（usage、CodexBar-Win），Tauri 适合追求极致体积的场景。
2. 没有现成项目能直接用（它们读的是 Claude Code/Codex 的日志或官方 API，不认 ZCode 的 SQLite）；但交互范式（悬浮球/置顶/穿透/阈值变色/托盘菜单/自启）全部可以照抄。
3. 我们 v1.0 已经把数据引擎做好了（只读 SQLite + SSE + 1 分钟推送），**悬浮框应该做成纯显示客户端**，避免重复实现数据层。

---

## 二、v2.0 实现方案

### 2.1 架构

```
~/.zcode/cli/db/db.sqlite ──► v1.0 本地服务（127.0.0.1:5323，看护模式常驻）
                                   │  SSE /api/events（2s 实时）
                                   ▼
                        桌面悬浮框 v2.0（纯显示客户端）
                        ├─ 置顶悬浮卡：剩余 tokens 大字 + 百分比 + 迷你进度条 + 重置 23:59
                        ├─ 数据：SSE 实时，断线退化为 2s 轮询；服务未启动显示灰色态 + 一键拉起
                        └─ 设置持久化 data/widget.json（位置/透明度/置顶/自启）
```

- 悬浮框不直接读 SQLite（保持单一数据引擎，v1/v2 数字永远一致）
- 服务由看护 bat 保障；悬浮框检测不到服务时提供「启动服务」按钮（拉起 bat）

### 2.2 功能清单

| 优先级 | 功能 |
|---|---|
| P0 | 悬浮卡 UI（剩余大字 + 百分比 + 迷你进度条 + 重置时间；视觉复用 v1.0 卡片样式）；置顶、无边框、可拖动、位置记忆、透明度可调 |
| P0 | 数据接入：SSE 实时 + 断线轮询兜底 + 离线状态提示；单击打开完整仪表盘（浏览器） |
| P1 | 悬停展开详情（已用/额度、分模型用量、最近更新时间）；右键菜单（置顶开关/透明度/开机自启/退出）；额度阈值变色（余量 <20% 黄、<10% 红，与 v1 一致）；系统托盘图标（tooltip 显示百分比） |
| P2 | 全局热键显隐；近 7 日迷你柱状；多供应商切换；二期可调研火山引擎 OpenAPI 直查服务端真实额度（token-monitor 项目即此做法，之前未查到公开接口，可再验证） |

### 2.3 技术栈选型（核心决策点）

| 方案 | 技术栈 | 优点 | 缺点 | 本机适配 |
|---|---|---|---|---|
| **A. Electron（推荐）** | Electron + 原生 HTML/CSS/JS（不引重框架） | npm 即装零新增工具链；**卡片视觉直接复用 v1.0 的 CSS**；置顶/无边框/透明/点击穿透/托盘/全局热键全部原生支持；同类项目主流路线 | 内存 ~100–150MB；目录 ~250MB（node_modules） | ✅ 完美（Node 25 已装） |
| B. Python 桌面 | PySide6（或 pywebview+WebView2） | 无 Node 依赖；内存略低；pip 即装 | 悬浮窗交互（穿透/hover 展开/右键）代码量更大；UI 样式需 QSS 重写；WebView2 本机状态未确认 | ✅ 可行（pip 装 PySide6 ≈ 400MB） |
| C. Tauri 2 | Rust + 系统 WebView | 体积内存最优（exe ~5MB / 内存 ~30MB）；最"原生" | **本机无 Rust 工具链**，需安装 rustup + MSVC Build Tools（约 2–3GB、一次性 20–40 分钟） | ⚠️ 需先装工具链 |
| D. Rainmeter 皮肤 | Rainmeter + WebParser | 零开发 | 需装 Rainmeter 平台；样式/交互受限；不算"我们的应用" | ➖ 不推荐主方案 |

### 2.4 里程碑（按方案 A 估算）

| 阶段 | 内容 | 预估 |
|---|---|---|
| M1 | Electron 骨架：透明置顶无边框窗口 + SSE/轮询数据接入 + 离线态 | 0.5 天 |
| M2 | 悬浮卡 UI（复用 v1.0 视觉）+ 拖动/位置记忆/透明度/右键菜单/悬停展开 | 0.5–1 天 |
| M3 | 托盘图标 + 开机自启（HKCU Run 注册表）+ 设置持久化 | 0.5 天 |
| M4 | electron-builder 打包便携 exe（可选）+ README/源码推送 | 0.5 天 |

### 2.5 风险与对策

| 风险 | 对策 |
|---|---|
| Electron 二进制下载慢/失败 | npm 装 electron 时设 `ELECTRON_MIRROR=https://npmmirror.com/mirrors/electron/`（npmmirror 已验证可达） |
| 远程桌面/缩放下透明窗口异常 | 提供不透明模式开关；DPI 感知声明 |
| 服务未运行时悬浮框无数据 | 离线态 + 「启动服务」按钮（拉起看护 bat）；可选自动拉起 |
| 开机自启权限 | 写 HKCU Run（用户级，无需管理员） |

---

## 三、待确认的问题

1. **技术栈**：A Electron（推荐）/ B PySide6 / C Tauri（愿意装 Rust 工具链的话长期最优）/ D Rainmeter？
2. **数据源**：悬浮框作为 v1.0 服务的纯显示客户端（推荐，数字与网页永远一致），还是内嵌直读 SQLite（可独立运行，但重复数据逻辑）？
3. **P1 功能取舍**：托盘图标、开机自启、悬停展开都要吗？
4. **默认偏好**：默认停靠屏幕右上角、不透明度 85%，可以吗？
